import styles from '@/app/index';

const VirtualTeaser = () => {
    return (
        <div className={styles.virtualTeaserSection}>
            Virtual TSYP Teaser
        </div>
    );
};

export default VirtualTeaser;
